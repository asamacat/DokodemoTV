---
description: Pull from GitHub and build the Android APK
---
// turbo-all
1. Run `git pull` to sync the codebase.
2. Run `./gradlew.bat assembleDebug --no-daemon` to build the APK.
3. Verify the output in `app/build/outputs/apk/debug/app-debug.apk`.
